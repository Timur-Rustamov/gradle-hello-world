node('slave1') {
  stage('checkout'){
    checkout scm
}
  stage ('build'){
  gradle = tool 'gradle4'
      			sh "${gradle}/bin/gradle build"
  }
  stage ('post'){
  addBadge(icon: "green.gif", text: "new badge") 
  }
}
  
